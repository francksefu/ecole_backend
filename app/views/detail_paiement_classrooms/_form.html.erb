<%= form_with(model: detail_paiement_classroom) do |form| %>
  <% if detail_paiement_classroom.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(detail_paiement_classroom.errors.count, "error") %> prohibited this detail_paiement_classroom from being saved:</h2>

      <ul>
        <% detail_paiement_classroom.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
