<%= form_with(model: evaluate) do |form| %>
  <% if evaluate.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(evaluate.errors.count, "error") %> prohibited this evaluate from being saved:</h2>

      <ul>
        <% evaluate.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :point, style: "display: block" %>
    <%= form.text_field :point %>
  </div>

  <div>
    <%= form.label :date_evaluation_id, style: "display: block" %>
    <%= form.text_field :date_evaluation_id %>
  </div>

  <div>
    <%= form.label :promotion_id, style: "display: block" %>
    <%= form.text_field :promotion_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
