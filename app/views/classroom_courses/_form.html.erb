<%= form_with(model: classroom_course) do |form| %>
  <% if classroom_course.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(classroom_course.errors.count, "error") %> prohibited this classroom_course from being saved:</h2>

      <ul>
        <% classroom_course.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
