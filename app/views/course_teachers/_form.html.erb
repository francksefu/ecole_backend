<%= form_with(model: course_teacher) do |form| %>
  <% if course_teacher.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(course_teacher.errors.count, "error") %> prohibited this course_teacher from being saved:</h2>

      <ul>
        <% course_teacher.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
